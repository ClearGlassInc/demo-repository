name: Proof HTML
on:
  push:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: anishathalye/proof-html@v1.1.0
        with:
          directory: ./
